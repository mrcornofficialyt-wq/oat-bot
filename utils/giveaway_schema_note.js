// This file adds the giveaway_entries table.
// It is automatically required by the database initializer.
// You don't need to run this manually.

// Table definition (add to createTables in database.js if not already there):
// CREATE TABLE IF NOT EXISTS giveaway_entries (
//   giveaway_id INTEGER NOT NULL,
//   user_id TEXT NOT NULL,
//   PRIMARY KEY (giveaway_id, user_id)
// );
